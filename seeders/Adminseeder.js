 //A new seeder file for adminseeder 

module.exports = {
        up: async function() {
        try {
                  console.log("test");
        } catch (error) {
            
        }
        },
};

        